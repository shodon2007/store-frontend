import { FC, memo } from "react";
import MyInput from "../../components/UI/input/MyInput";

const DeleteDevice: FC = memo(() => {
    return (
        <div>
            <MyInput />
        </div>
    );
});

export default DeleteDevice;
